package sc.init;

/** 
 * Extend this layer for a simple main based app if there are no other framework layers like swing, servlets.
 * It implements support for initializing the default types and objects, enabling components or layers like the db 
 * data sources that use those features when used in a stand-alone application. 
 */
public sys.basicMain {
}

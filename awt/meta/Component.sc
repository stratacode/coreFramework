Component {
/* TODO: these properties are often made bindable in concrete subclasses
  See the annotation layer for the swing JComponent class for a way to 
  mark the properties as bindable with manual=true (and maybe we should
  apply that same pattern here to avoid binding warnings when a ref to
  an awt component is used in a binding expression)
  override @Bindable size;
  override @Bindable location;
*/
}
